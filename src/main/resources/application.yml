spring:
  application:
    name: sweldox
  sql:
    init:
      mode: always

server:
  port: ${PORT}
  servlet:
    context-path: /api

springdoc:
  api-docs:
    path: /docs
  swagger-ui:
    path: /swagger-ui.html
  show-actuator: false

security:
  jwt:
    secret-key: ${JWT_SECRET}
    expiration-time: ${JWT_EXPIRATION}